using UnityEngine;

public class TestScript2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("TestScript2: Hello from Claude via WSL!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
