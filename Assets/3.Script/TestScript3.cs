using UnityEngine;

public class TestScript3 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("TestScript3: Hello from Claude at school!");
        Debug.Log("TestScript3: Filesystem MCP is working perfectly!");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
